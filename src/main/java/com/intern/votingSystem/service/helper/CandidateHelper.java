package com.intern.votingSystem.service.helper;

import com.intern.votingSystem.model.Candidate;

public class CandidateHelper {
//    public static Candidate prepareCandidateResponse(Candidate candidate){
//return Candidate.builder().c_id(candidate.getC_id()).candidateName(candidate.getCandidateName()).candidateCode(candidate.getCandidateCode()).vote(candidate.getVote()).build();
//    }
}
